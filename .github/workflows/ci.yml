name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  check:
    name: lint, typecheck, test, build
    runs-on: ubuntu-latest
    timeout-minutes: 15

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: mastro
          POSTGRES_PASSWORD: mastro
          POSTGRES_DB: mastro
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U mastro -d mastro"
          --health-interval 2s
          --health-timeout 3s
          --health-retries 30

    env:
      DATABASE_URL: postgres://mastro:mastro@localhost:5432/mastro

    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm check

      - name: Migrate
        run: pnpm db:migrate

      # Includes the invariant tests: no country-specific logic outside a
      # jurisdiction pack, and no missing translation key.
      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build
